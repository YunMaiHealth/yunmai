/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { MessageNotifyService } from "../messageNotify.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MessageNotifyCreateInput } from "./MessageNotifyCreateInput";
import { MessageNotifyWhereInput } from "./MessageNotifyWhereInput";
import { MessageNotifyWhereUniqueInput } from "./MessageNotifyWhereUniqueInput";
import { MessageNotifyFindManyArgs } from "./MessageNotifyFindManyArgs";
import { MessageNotifyUpdateInput } from "./MessageNotifyUpdateInput";
import { MessageNotify } from "./MessageNotify";
import { EventLogFindManyArgs } from "../../eventLog/base/EventLogFindManyArgs";
import { EventLog } from "../../eventLog/base/EventLog";
import { EventLogWhereUniqueInput } from "../../eventLog/base/EventLogWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class MessageNotifyControllerBase {
  constructor(
    protected readonly service: MessageNotifyService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: MessageNotify })
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: MessageNotifyCreateInput
  ): Promise<MessageNotify> {
    return await this.service.create({
      data: {
        ...data,

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        id: true,
        sendTime: true,
        updatedAt: true,
        messageSource: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [MessageNotify] })
  @ApiNestedQuery(MessageNotifyFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<MessageNotify[]> {
    const args = plainToClass(MessageNotifyFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        sendTime: true,
        updatedAt: true,
        messageSource: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: MessageNotify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: MessageNotifyWhereUniqueInput
  ): Promise<MessageNotify | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        sendTime: true,
        updatedAt: true,
        messageSource: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: MessageNotify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: MessageNotifyWhereUniqueInput,
    @common.Body() data: MessageNotifyUpdateInput
  ): Promise<MessageNotify | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          id: true,
          sendTime: true,
          updatedAt: true,
          messageSource: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: MessageNotify })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: MessageNotifyWhereUniqueInput
  ): Promise<MessageNotify | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          sendTime: true,
          updatedAt: true,
          messageSource: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/eventLogs")
  @ApiNestedQuery(EventLogFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "EventLog",
    action: "read",
    possession: "any",
  })
  async findManyEventLogs(
    @common.Req() request: Request,
    @common.Param() params: MessageNotifyWhereUniqueInput
  ): Promise<EventLog[]> {
    const query = plainToClass(EventLogFindManyArgs, request.query);
    const results = await this.service.findEventLogs(params.id, {
      ...query,
      select: {
        id: true,
        eventTime: true,

        messageNotifies: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/eventLogs")
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "update",
    possession: "any",
  })
  async connectEventLogs(
    @common.Param() params: MessageNotifyWhereUniqueInput,
    @common.Body() body: EventLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      eventLogs: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/eventLogs")
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "update",
    possession: "any",
  })
  async updateEventLogs(
    @common.Param() params: MessageNotifyWhereUniqueInput,
    @common.Body() body: EventLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      eventLogs: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/eventLogs")
  @nestAccessControl.UseRoles({
    resource: "MessageNotify",
    action: "update",
    possession: "any",
  })
  async disconnectEventLogs(
    @common.Param() params: MessageNotifyWhereUniqueInput,
    @common.Body() body: EventLogWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      eventLogs: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
