/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { UserQuestionService } from "../userQuestion.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { UserQuestionCreateInput } from "./UserQuestionCreateInput";
import { UserQuestionWhereInput } from "./UserQuestionWhereInput";
import { UserQuestionWhereUniqueInput } from "./UserQuestionWhereUniqueInput";
import { UserQuestionFindManyArgs } from "./UserQuestionFindManyArgs";
import { UserQuestionUpdateInput } from "./UserQuestionUpdateInput";
import { UserQuestion } from "./UserQuestion";
import { ReplyQuestionFindManyArgs } from "../../replyQuestion/base/ReplyQuestionFindManyArgs";
import { ReplyQuestion } from "../../replyQuestion/base/ReplyQuestion";
import { ReplyQuestionWhereUniqueInput } from "../../replyQuestion/base/ReplyQuestionWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class UserQuestionControllerBase {
  constructor(
    protected readonly service: UserQuestionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: UserQuestion })
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: UserQuestionCreateInput
  ): Promise<UserQuestion> {
    return await this.service.create({
      data: {
        ...data,

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        id: true,
        questionTime: true,
        questionTitle: true,
        questionContent: true,
        isPublic: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [UserQuestion] })
  @ApiNestedQuery(UserQuestionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<UserQuestion[]> {
    const args = plainToClass(UserQuestionFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        questionTime: true,
        questionTitle: true,
        questionContent: true,
        isPublic: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: UserQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: UserQuestionWhereUniqueInput
  ): Promise<UserQuestion | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        questionTime: true,
        questionTitle: true,
        questionContent: true,
        isPublic: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: UserQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: UserQuestionWhereUniqueInput,
    @common.Body() data: UserQuestionUpdateInput
  ): Promise<UserQuestion | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          id: true,
          questionTime: true,
          questionTitle: true,
          questionContent: true,
          isPublic: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: UserQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: UserQuestionWhereUniqueInput
  ): Promise<UserQuestion | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          questionTime: true,
          questionTitle: true,
          questionContent: true,
          isPublic: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/replyQuestions")
  @ApiNestedQuery(ReplyQuestionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "read",
    possession: "any",
  })
  async findManyReplyQuestions(
    @common.Req() request: Request,
    @common.Param() params: UserQuestionWhereUniqueInput
  ): Promise<ReplyQuestion[]> {
    const query = plainToClass(ReplyQuestionFindManyArgs, request.query);
    const results = await this.service.findReplyQuestions(params.id, {
      ...query,
      select: {
        id: true,
        replyTime: true,
        questionReply: true,
        isPublic: true,

        replyUser: {
          select: {
            id: true,
          },
        },

        userQuestion: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/replyQuestions")
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "update",
    possession: "any",
  })
  async connectReplyQuestions(
    @common.Param() params: UserQuestionWhereUniqueInput,
    @common.Body() body: ReplyQuestionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      replyQuestions: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/replyQuestions")
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "update",
    possession: "any",
  })
  async updateReplyQuestions(
    @common.Param() params: UserQuestionWhereUniqueInput,
    @common.Body() body: ReplyQuestionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      replyQuestions: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/replyQuestions")
  @nestAccessControl.UseRoles({
    resource: "UserQuestion",
    action: "update",
    possession: "any",
  })
  async disconnectReplyQuestions(
    @common.Param() params: UserQuestionWhereUniqueInput,
    @common.Body() body: ReplyQuestionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      replyQuestions: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
