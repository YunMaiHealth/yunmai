/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { IncomeMataGasService } from "../incomeMataGas.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { IncomeMataGasCreateInput } from "./IncomeMataGasCreateInput";
import { IncomeMataGasWhereInput } from "./IncomeMataGasWhereInput";
import { IncomeMataGasWhereUniqueInput } from "./IncomeMataGasWhereUniqueInput";
import { IncomeMataGasFindManyArgs } from "./IncomeMataGasFindManyArgs";
import { IncomeMataGasUpdateInput } from "./IncomeMataGasUpdateInput";
import { IncomeMataGas } from "./IncomeMataGas";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class IncomeMataGasControllerBase {
  constructor(
    protected readonly service: IncomeMataGasService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: IncomeMataGas })
  @nestAccessControl.UseRoles({
    resource: "IncomeMataGas",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: IncomeMataGasCreateInput
  ): Promise<IncomeMataGas> {
    return await this.service.create({
      data: {
        ...data,

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        id: true,
        income_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [IncomeMataGas] })
  @ApiNestedQuery(IncomeMataGasFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "IncomeMataGas",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<IncomeMataGas[]> {
    const args = plainToClass(IncomeMataGasFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        income_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: IncomeMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IncomeMataGas",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: IncomeMataGasWhereUniqueInput
  ): Promise<IncomeMataGas | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        income_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: IncomeMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IncomeMataGas",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: IncomeMataGasWhereUniqueInput,
    @common.Body() data: IncomeMataGasUpdateInput
  ): Promise<IncomeMataGas | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          id: true,
          income_date: true,

          user: {
            select: {
              id: true,
            },
          },

          amount: true,
          category: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: IncomeMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "IncomeMataGas",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: IncomeMataGasWhereUniqueInput
  ): Promise<IncomeMataGas | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          income_date: true,

          user: {
            select: {
              id: true,
            },
          },

          amount: true,
          category: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
