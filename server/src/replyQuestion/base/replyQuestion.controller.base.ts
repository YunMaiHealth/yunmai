/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ReplyQuestionService } from "../replyQuestion.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ReplyQuestionCreateInput } from "./ReplyQuestionCreateInput";
import { ReplyQuestionWhereInput } from "./ReplyQuestionWhereInput";
import { ReplyQuestionWhereUniqueInput } from "./ReplyQuestionWhereUniqueInput";
import { ReplyQuestionFindManyArgs } from "./ReplyQuestionFindManyArgs";
import { ReplyQuestionUpdateInput } from "./ReplyQuestionUpdateInput";
import { ReplyQuestion } from "./ReplyQuestion";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ReplyQuestionControllerBase {
  constructor(
    protected readonly service: ReplyQuestionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ReplyQuestion })
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: ReplyQuestionCreateInput
  ): Promise<ReplyQuestion> {
    return await this.service.create({
      data: {
        ...data,

        replyUser: {
          connect: data.replyUser,
        },

        userQuestion: data.userQuestion
          ? {
              connect: data.userQuestion,
            }
          : undefined,
      },
      select: {
        id: true,
        replyTime: true,
        questionReply: true,
        isPublic: true,

        replyUser: {
          select: {
            id: true,
          },
        },

        userQuestion: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ReplyQuestion] })
  @ApiNestedQuery(ReplyQuestionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<ReplyQuestion[]> {
    const args = plainToClass(ReplyQuestionFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        replyTime: true,
        questionReply: true,
        isPublic: true,

        replyUser: {
          select: {
            id: true,
          },
        },

        userQuestion: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ReplyQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: ReplyQuestionWhereUniqueInput
  ): Promise<ReplyQuestion | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        replyTime: true,
        questionReply: true,
        isPublic: true,

        replyUser: {
          select: {
            id: true,
          },
        },

        userQuestion: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ReplyQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: ReplyQuestionWhereUniqueInput,
    @common.Body() data: ReplyQuestionUpdateInput
  ): Promise<ReplyQuestion | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          replyUser: {
            connect: data.replyUser,
          },

          userQuestion: data.userQuestion
            ? {
                connect: data.userQuestion,
              }
            : undefined,
        },
        select: {
          id: true,
          replyTime: true,
          questionReply: true,
          isPublic: true,

          replyUser: {
            select: {
              id: true,
            },
          },

          userQuestion: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ReplyQuestion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ReplyQuestion",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: ReplyQuestionWhereUniqueInput
  ): Promise<ReplyQuestion | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          replyTime: true,
          questionReply: true,
          isPublic: true,

          replyUser: {
            select: {
              id: true,
            },
          },

          userQuestion: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
