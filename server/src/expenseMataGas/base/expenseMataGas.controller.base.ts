/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ExpenseMataGasService } from "../expenseMataGas.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ExpenseMataGasCreateInput } from "./ExpenseMataGasCreateInput";
import { ExpenseMataGasWhereInput } from "./ExpenseMataGasWhereInput";
import { ExpenseMataGasWhereUniqueInput } from "./ExpenseMataGasWhereUniqueInput";
import { ExpenseMataGasFindManyArgs } from "./ExpenseMataGasFindManyArgs";
import { ExpenseMataGasUpdateInput } from "./ExpenseMataGasUpdateInput";
import { ExpenseMataGas } from "./ExpenseMataGas";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ExpenseMataGasControllerBase {
  constructor(
    protected readonly service: ExpenseMataGasService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ExpenseMataGas })
  @nestAccessControl.UseRoles({
    resource: "ExpenseMataGas",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: ExpenseMataGasCreateInput
  ): Promise<ExpenseMataGas> {
    return await this.service.create({
      data: {
        ...data,

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        id: true,
        expense_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ExpenseMataGas] })
  @ApiNestedQuery(ExpenseMataGasFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ExpenseMataGas",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<ExpenseMataGas[]> {
    const args = plainToClass(ExpenseMataGasFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        expense_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ExpenseMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExpenseMataGas",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: ExpenseMataGasWhereUniqueInput
  ): Promise<ExpenseMataGas | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        expense_date: true,

        user: {
          select: {
            id: true,
          },
        },

        amount: true,
        category: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ExpenseMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExpenseMataGas",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: ExpenseMataGasWhereUniqueInput,
    @common.Body() data: ExpenseMataGasUpdateInput
  ): Promise<ExpenseMataGas | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          id: true,
          expense_date: true,

          user: {
            select: {
              id: true,
            },
          },

          amount: true,
          category: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ExpenseMataGas })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ExpenseMataGas",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: ExpenseMataGasWhereUniqueInput
  ): Promise<ExpenseMataGas | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          expense_date: true,

          user: {
            select: {
              id: true,
            },
          },

          amount: true,
          category: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
